pipeline {
    agent any

    stages {
        stage('Preparation') {
            echo "I'm on the first branch!"
        }
        
        stage('Test') {
            echo "I'm on the first branch!"
        }
        
        stage('Publish') {
            echo "I'm on the first branch!"   
        }
    }
}